/**
 * Restock an npc's store
 * 
 * execute with a jenkins scheduler
 *   node restock.js < NPCInventory.json
 */

// get database connection

// read job definition

// handle each item in the definition, randomly giving a variable amount to
// the shop keeper to sell
